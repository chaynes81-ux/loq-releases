<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 630">
  <defs>
    <linearGradient id="bg" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#1a365d"/>
      <stop offset="100%" style="stop-color:#2d5a87"/>
    </linearGradient>
  </defs>

  <!-- Background -->
  <rect width="1200" height="630" fill="url(#bg)"/>

  <!-- Main title -->
  <text x="600" y="240" font-family="system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif" font-size="120" font-weight="800" fill="white" text-anchor="middle">loq</text>

  <!-- Subtitle -->
  <text x="600" y="320" font-family="system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif" font-size="48" font-weight="400" fill="#94a3b8" text-anchor="middle">Log Query</text>

  <!-- Tagline -->
  <text x="600" y="420" font-family="system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif" font-size="32" font-weight="400" fill="#cbd5e1" text-anchor="middle">SQL-based log analysis for Windows, macOS &amp; Linux</text>

  <!-- Bottom tagline -->
  <text x="600" y="480" font-family="system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif" font-size="28" font-weight="400" fill="#64748b" text-anchor="middle">A modern replacement for Microsoft Log Parser 2.2</text>
</svg>